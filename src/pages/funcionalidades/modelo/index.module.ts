import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ModelosPage } from './';

@NgModule({
  declarations: [
    ModelosPage,
  ],
  imports: [
    IonicPageModule.forChild(ModelosPage),
  ],
})
export class ModelosPageModule {}
