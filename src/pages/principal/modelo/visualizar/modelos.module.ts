import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ModelosPage } from './modelos';

@NgModule({
  declarations: [
    ModelosPage,
  ],
  imports: [
    IonicPageModule.forChild(ModelosPage),
  ],
})
export class ModelosPageModule {}
